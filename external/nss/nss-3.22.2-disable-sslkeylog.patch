diff -ru nss-3.22.2.orig/nss/lib/ssl/sslsock.c nss-3.22.2/nss/lib/ssl/sslsock.c
--- a/a/nss/lib/ssl/sslsock.c	2016-02-26 10:49:58.000000000 +0100
+++ b/b/nss/lib/ssl/sslsock.c	2016-04-28 08:46:03.499386345 +0200
@@ -3352,19 +3352,6 @@
             SSL_TRACE(("SSL: debugging set to %d", ssl_debug));
         }
 #endif /* DEBUG */
-        ev = PR_GetEnvSecure("SSLKEYLOGFILE");
-        if (ev && ev[0]) {
-            ssl_keylog_iob = fopen(ev, "a");
-            if (!ssl_keylog_iob) {
-                SSL_TRACE(("SSL: failed to open key log file"));
-            } else {
-                if (ftell(ssl_keylog_iob) == 0) {
-                    fputs("# SSL/TLS secrets log file, generated by NSS\n",
-                          ssl_keylog_iob);
-                }
-                SSL_TRACE(("SSL: logging SSL/TLS secrets to %s", ev));
-            }
-        }
 #ifndef NO_PKCS11_BYPASS
         ev = PR_GetEnvSecure("SSLBYPASS");
         if (ev && ev[0]) {
Only in nss-3.22.2/nss/lib/ssl: sslsock.c~
